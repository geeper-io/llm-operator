---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: deployments.llm.geeper.io
spec:
  group: llm.geeper.io
  names:
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: string
    - jsonPath: .status.totalReplicas
      name: Total
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Deployment is the Schema for the ollamadeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeploymentSpec defines the desired state of Deployment
            properties:
              ollama:
                description: Ollama defines the Ollama deployment configuration
                properties:
                  image:
                    description: Image is the Ollama container image to use
                    type: string
                  imageTag:
                    description: ImageTag is the Ollama image tag to use
                    type: string
                  models:
                    description: Models is the list of models to deploy with Ollama
                    items:
                      description: |-
                        OllamaModel defines a model to be deployed with Ollama
                        Format: "modelname:tag" (e.g., "llama2:7b", "mistral:7b", "codellama:13b")
                      type: string
                    type: array
                  replicas:
                    description: Replicas is the number of Ollama pods to run
                    format: int32
                    maximum: 10
                    minimum: 1
                    type: integer
                  resources:
                    description: Resources defines the resource requirements for Ollama
                      pods
                    properties:
                      limits:
                        description: Limits describes the maximum amount of compute
                          resources allowed
                        properties:
                          cpu:
                            description: CPU is the CPU resource (e.g., "100m", "2")
                            type: string
                          memory:
                            description: Memory is the memory resource (e.g., "128Mi",
                              "2Gi")
                            type: string
                          storage:
                            description: Storage is the storage resource (e.g., "1Gi",
                              "100Gi")
                            type: string
                        type: object
                      requests:
                        description: Requests describes the minimum amount of compute
                          resources required
                        properties:
                          cpu:
                            description: CPU is the CPU resource (e.g., "100m", "2")
                            type: string
                          memory:
                            description: Memory is the memory resource (e.g., "128Mi",
                              "2Gi")
                            type: string
                          storage:
                            description: Storage is the storage resource (e.g., "1Gi",
                              "100Gi")
                            type: string
                        type: object
                    type: object
                  servicePort:
                    description: ServicePort is the port to expose Ollama service
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  serviceType:
                    description: ServiceType is the type of service to expose Ollama
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                required:
                - models
                type: object
              openwebui:
                description: OpenWebUI defines the OpenWebUI deployment configuration
                properties:
                  enabled:
                    description: Enabled determines if OpenWebUI should be deployed
                    type: boolean
                  image:
                    description: Image is the OpenWebUI container image to use
                    type: string
                  imageTag:
                    description: ImageTag is the OpenWebUI image tag to use
                    type: string
                  ingressEnabled:
                    description: IngressEnabled determines if an Ingress should be
                      created
                    type: boolean
                  ingressHost:
                    description: IngressHost is the hostname for the Ingress
                    type: string
                  replicas:
                    description: Replicas is the number of OpenWebUI pods to run
                    format: int32
                    maximum: 5
                    minimum: 1
                    type: integer
                  resources:
                    description: Resources defines the resource requirements for OpenWebUI
                      pods
                    properties:
                      limits:
                        description: Limits describes the maximum amount of compute
                          resources allowed
                        properties:
                          cpu:
                            description: CPU is the CPU resource (e.g., "100m", "2")
                            type: string
                          memory:
                            description: Memory is the memory resource (e.g., "128Mi",
                              "2Gi")
                            type: string
                          storage:
                            description: Storage is the storage resource (e.g., "1Gi",
                              "100Gi")
                            type: string
                        type: object
                      requests:
                        description: Requests describes the minimum amount of compute
                          resources required
                        properties:
                          cpu:
                            description: CPU is the CPU resource (e.g., "100m", "2")
                            type: string
                          memory:
                            description: Memory is the memory resource (e.g., "128Mi",
                              "2Gi")
                            type: string
                          storage:
                            description: Storage is the storage resource (e.g., "1Gi",
                              "100Gi")
                            type: string
                        type: object
                    type: object
                  servicePort:
                    description: ServicePort is the port to expose OpenWebUI service
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  serviceType:
                    description: ServiceType is the type of service to expose OpenWebUI
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
            required:
            - ollama
            type: object
          status:
            description: DeploymentStatus defines the observed state of Deployment
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the deployment's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              ollamaStatus:
                description: OllamaStatus represents the status of Ollama deployment
                properties:
                  availableReplicas:
                    description: AvailableReplicas is the number of available replicas
                    format: int32
                    type: integer
                  conditions:
                    description: Conditions represent the latest available observations
                      of the component's current state
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                  readyReplicas:
                    description: ReadyReplicas is the number of ready replicas
                    format: int32
                    type: integer
                  updatedReplicas:
                    description: UpdatedReplicas is the number of updated replicas
                    format: int32
                    type: integer
                type: object
              openwebuiStatus:
                description: OpenWebUIStatus represents the status of OpenWebUI deployment
                properties:
                  availableReplicas:
                    description: AvailableReplicas is the number of available replicas
                    format: int32
                    type: integer
                  conditions:
                    description: Conditions represent the latest available observations
                      of the component's current state
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                  readyReplicas:
                    description: ReadyReplicas is the number of ready replicas
                    format: int32
                    type: integer
                  updatedReplicas:
                    description: UpdatedReplicas is the number of updated replicas
                    format: int32
                    type: integer
                type: object
              phase:
                description: Phase represents the current phase of the deployment
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready replicas
                format: int32
                type: integer
              totalReplicas:
                description: TotalReplicas is the total number of replicas
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
